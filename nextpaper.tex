\section{Next Paper}
\label{sec:nextpaper}
The Checker Framework manual advises developers to ``rewrite your code to be simpler for the checker to analyze; this is likely to make it easier for people to understand, too''~\cite{checkerframework-manual}. Similarly, the documentation of the OpenJML verification tool notes that ``success in checking the consistency of the specifications and the code will depend on the complexity and style in which the code and specifications are written''~\cite{openjml}. These statements reflect a widely held assumption in the software verification community that code that is easier for verification tools to analyze is also easier for humans to understand.

Although this relationship has been widely assumed, prior empirical evidence remains limited in its ability to characterize it under controlled conditions. Recent work~\cite{FeldmanKC2023} investigated this hypothesis by defining verifiability as the effort required to establish safety properties (e.g., the absence of null pointer dereferences or out-of-bounds accesses) using verification tools, and operationalizing it via the presence or absence of false positive warnings. That work conducted a meta-analysis over code snippets from prior comprehensibility studies, aggregating results across different tools, warning types, and experimental settings, and found associations between verification-related signals and measures of human understanding.

However, because the analysis aggregates results from studies that were not designed to isolate this relationship, several sources of variation remain entangled. In particular, variation in code structure, diversity of warning types, and differences in the underlying causes of warnings were not systematically controlled. In addition, the underlying studies employed a wide range of proxies to approximate code comprehensibility, many of which have since been shown in our recent work (Section~\ref{ase}) to be weak or unreliable proxies for human understanding. Consequently, the aggregated signals in the meta-analysis may reflect multiple interacting influences, including both differences in verification-related properties and inconsistencies in how comprehensibility was measured, and therefore do not provide a clear understanding of how verifiability relates to comprehensibility under controlled conditions.

To better understand this relationship, we propose a controlled experimental study that systematically manipulates verifiability while constraining other sources of variation as much as possible. We fix the verification setting by restricting the study to a small, predefined set of verification tools selected based on expert judgment, ensuring that all code is analyzed under a consistent and well-defined configuration. Within this setting, we construct a dataset of semantically equivalent code snippets, where each snippet is represented by two versions: a high-verifiability version that produces no warnings under the selected verification tools, and a low-verifiability version that produces false positive warnings under the same tools. These versions are created using carefully selected, semantics-preserving transformations derived from documented causes of false positives, allowing us to vary verifiability while keeping the underlying functionality of the code constant. To further control for confounding factors, we select transformations that minimally affect code length and control flow, and mitigate participant- and task-level variability through randomized assignment of snippet versions and appropriate statistical modeling.

Participants then complete input-output comprehension tasks on these snippets. For each snippet, they are given a concrete input and asked to determine the corresponding output of the method. Based on prior work, we use the time required to correctly answer these questions as a validated proxy for code comprehensibility. By collecting response times across multiple snippets and participants and analyzing the data using appropriate statistical models, we examine how differences in verifiability are reflected in measures of human understanding.

Our main research question is:

  \begin{researchquestions}
  \item Are semantically equivalent code snippets with higher verifiability more comprehensible than those with lower verifiability?
  \end{researchquestions}
  
 If a strong relationship is established, verifiability could serve as a practical automatic proxy, reducing reliance on costly human studies and enabling scalable tools for improving code quality. Even a moderate relationship would support incorporating verifiability as a component in multi-factor proxies.

 If a substantial relationship between code comprehensibility and verifiability exists, and if a reliable automatic proxy for verifiability can be used could be used to identify code that is difficult to understand and guide refactoring toward more comprehensible forms. This is particularly important given that prior studies estimate that developers spend between 58\% and 70\% of their time understanding code\todo{cite}. Improving code comprehensibility could therefore have a significant impact on developer productivity. Moreover, with the increasing use of large language models for code generation, such proxies could help ensure that generated code remains understandable to human developers.

  
\subsection{Background}
\subsubsection{Code Verifiability}

We define code verifiability as the effort required for a developer to use a verification tool to establish the correctness of a code snippet with respect to a set of safety properties. In practice, verifiability is not an intrinsic property of code independent of context, but is defined relative to a specific verification setting, including the tool, the properties being checked, and the available specifications.

Directly measuring verification effort would require controlled studies with developers actively using verification tools, which is difficult to scale. Therefore, following prior work, we operationalize verifiability using an automated proxy based on the behavior of static analysis tools\todo{We have to be careful here not to include non-verification static analysis tools, such as heuristic bug-finding tools. Make sure that this definition includes a notion of soundness or ``building a proof internally''. Maybe base it on the presence or absence of theoretically sound deductive reasoning? That's what we did for the FSE 2023 paper: Infer counts, because its underlying analysis is biabduction (a proof technique), even though the tool itself is unsound, because it uses heuristics to decide which warnings to report. Given the study design below, though, I think you can get away with ``sound'': CF checkers are (supposed to be) sound.}.

In this study, we focus on the Checker Framework and select a small number of its checkers. We choose the Checker Framework because it is open-source, actively maintained, and widely used for verification in Java. It provides a collection of pluggable type checkers (each of which we treat as a verifier in our setting) that are designed to be sound in the sense that they aim to detect all violations of a well-defined class of properties. However, because many interesting program properties are undecidable~\cite{rice1953classes}, such sound verifiers may produce false positive warnings; that is, they conservatively issue warnings when they cannot establish a proof. In addition, and importantly for our study, we have access to a domain expert on the Checker Framework within our team. This enables us to design our treatments in a more controlled manner, for example by helping distinguish false positive warnings from true defects more reliably.


We then operationalize verifiability in terms of the presence or absence of false positive warnings produced by the chosen checkers.

A false positive warning occurs when the tool reports a potential violation of a safety property in code that is in fact correct. Such warnings typically arise because the tool is unable to infer necessary facts about the program due to limitations in its analysis or due to implicit assumptions in the code. Code that produces false positive warnings requires additional effort from developers, who must either refactor the code, provide additional specifications, or manually inspect and dismiss the warning.

To enable controlled manipulation, we define verifiability as a binary condition:
\begin{itemize}
    \item high verifiability corresponds to code that produces no warnings,
    \item low verifiability corresponds to semantically equivalent code that produces false positive warnings.
\end{itemize}

This operationalization allows us to construct paired code snippets that differ only in whether the checker can successfully verify them.

\subsubsection{Code Comprehensibility}

In prior work~\ref{ase}, we defined code comprehensibility operationally through expert agreement on the relative difficulty of understanding code snippets. Using a Delphi-style protocol, multiple experienced developers iteratively ranked snippets by comprehension difficulty and resolved disagreements through structured feedback. The resulting rankings provided a practical approximation of ground-truth comprehensibility. Because expert-based evaluation is costly and does not scale, we evaluated a range of proxy measures using student studies. The results showed that proxies based on input-output reasoning about program behavior align most strongly with expert judgments. In particular, the time required to correctly answer input-output questions was the most reliable proxy.

Based on these findings, we adopt input-output comprehension tasks and use response time for correct answers as the primary measure of comprehensibility in this study. This measure captures the effort required to understand the behavior of the code while remaining scalable and suitable for controlled experiments.

\subsection{Methodology}

\subsubsection{Study Overview}

The goal of this study is to estimate the effect of code verifiability on code comprehensibility under a controlled verification setting\todo{I'm not sure if you mean ``verification'' here in the technical sense. If so, I wouldn't use it here; the ``controlled'' part of the experiment isn't only verification-related.}. We conduct a controlled experiment in which participants perform input-output comprehension tasks on semantically equivalent versions of Java methods that differ only in their verifiability with respect to the \textit{Checker Framework}\todo{I'm not sure what the italics here are intended to convey}. Verifiability is operationalized relative to a fixed verification ecosystem, and is manipulated through semantics-preserving code transformations that induce or eliminate false positive warnings produced by selected checkers.\todo{Include an example of a snippet with/without the treatment in a figure.}

The study is designed as a multi-item, between-subjects experiment with repeated measures, where each participant completes multiple tasks and each code snippet appears in two versions across participants. The analysis is conducted using mixed-effects models to account for variability across participants and snippets.\todo{Justify this choice: why can't you use a within-subjects design, which is cheaper?}\todo{How do you know you will have sufficient discriminatory power with this design to find an effect? How many students are you planning to run this experiment on?}

\subsubsection{Independent Variable and Treatment Definition}

The primary independent variable is \textit{verifiability condition}, defined relative to a fixed verification setting based on the Checker Framework.

This variable is manipulated as a binary factor with two levels:
\begin{itemize}
    \item \textbf{High-verifiability condition}: the code snippet produces no false positive warnings under the selected checker.
    \item \textbf{Low-verifiability condition}: a semantically equivalent version of the same snippet produces one or more false positive warnings under the same checker.
\end{itemize}

The treatment is therefore not an abstract notion of verifiability, but a concrete manipulation of code that changes whether the checker can successfully verify certain safety properties. This manipulation is implemented through controlled code transformations that preserve program semantics while affecting the ability of the checker to reason about the code. 

We define semantics preservation in terms of input--output behavior: for any valid input, the transformed program produces the same observable output (including return values and externally visible side effects) as the original program. Our transformations are not required to preserve lower-level properties such as memory layout, evaluation order, or intermediate states, as long as the externally observable behavior remains unchanged.

\subsubsection{Dependent Variable}

The dependent variable is \textit{code comprehensibility}, operationalized using a validated proxy measure.

The primary outcome is the \textit{time required to correctly answer an input-output comprehension question} for a given code snippet. This measure captures the effort required for a participant to understand the behavior of the code. Time is recorded from the moment the participant begins the task until a correct answer is submitted.

Correctness is also recorded as a secondary measure, and time-based analyses focus primarily on correct responses\todo{What does ``focus primarily'' mean? Are you excluding the answers from students who get the answers wrong? I don't think there's a justification for doing so, since in our ASE26 work I don't think we did that; if we did, some of the writing is almost certainly making false claims.}.

\subsubsection{Verification Setting and Scope Conditions}

The study fixes several aspects of the verification context to ensure a well-defined construct of verifiability.

The verification ecosystem is restricted to the Checker Framework. Within this framework, we select at most two checkers based on expert judgment, prioritizing checkers that are widely used and that produce meaningful safety warnings. For each selected checker, we focus on its most frequent and representative warning types, as identified from the checker documentation and associated research papers.\todo{I think you should make these choices now, or at least choose one checker to definitely include, so that you can include concrete examples. I suggest the Nullness Checker or NullAway.}

These choices define the scope of the study. The results are therefore interpreted as applying to verifiability within the selected Checker Framework setting, rather than to all possible verification tools or warning types.

\subsubsection{Treatment Realization: Code Transformations}

The manipulation of verifiability is achieved through semantics-preserving code transformations that induce false positive warnings.

To illustrate the type of transformation we consider, we present a simplified example based on the index checker from the Checker Framework (Figure~\ref{fig:verifiability-example}). The low-verifiability version produces two false positive warnings: \lstinline{[array.access.unsafe.low]} and \lstinline{[array.access.unsafe.high]}. This occurs because the bounds check is performed in a helper method, preventing the checker from refining constraints across method boundaries. As a result, the checker cannot establish a proof and therefore conservatively issues warnings to preserve soundness. 

In contrast, the high-verifiability version produces no warnings because the bounds check is performed directly within the conditional, allowing the checker to refine the value of \lstinline{i} within the guarded block and verify that the array access is safe.

\begin{figure}[t]
\centering
\begin{minipage}{0.48\textwidth}
\textbf{Low-verifiability version}
\begin{lstlisting}[language=Java]
static boolean isSafe(int i, int[] a) {
    return i >= 1 && i < a.length;
}

static void f(int[] a, int i) {
    if (isSafe(i, a)) {
        int x = a[i]; // Warning
    }
}
\end{lstlisting}
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\textbf{High-verifiability version}
\begin{lstlisting}[language=Java]
static void f(int[] a, int i) {
    if (i >= 1 && i < a.length) {
        int x = a[i]; // No warning
    }
}
\end{lstlisting}
\end{minipage}
\caption{Example of a semantics-preserving transformation that affects verifiability. Encapsulating a safety condition in a helper method prevents the checker from refining constraints across method boundaries, leading to false positive warnings.}
\label{fig:verifiability-example}
\end{figure}

We construct a set of candidate transformations using the following process. First, we review the research papers and official documentation\todo{but not the bug trackers? Whyever not? Bug trackers should definitely be included, among the others} of the selected checkers to identify common causes of false positive warnings. These sources typically describe patterns that hinder the checker’s ability to infer required properties. If necessary, we supplement this information by mining real-world repositories, searching for commits or issues that reference false positives associated with the selected checkers.\todo{Bug trackers are ok, but I don't think you should even begin to suggest that you will try to mine these patterns from real repositories. That's an interesting project in-and-of itself and would be fun to do, but not as part of this study: \url{https://docs.google.com/document/d/1vhhb3SJESImnxMA0q196fmZp_1vHsj9X9Q9ylmU8i14/edit?usp=sharing}. You can participate if we recruit an undergrad and you're interested.}

From these sources, we derive a set of candidate code transformations that:
\begin{enumerate}
    \item preserve the semantics of the original program,
    \item reliably induce or eliminate false positive warnings,
    \item have minimal impact on code length and structure, and
    \item can be applied to multiple self-contained Java methods.
\end{enumerate}

We select a small number of such transformations for each checker, ensuring that the total number of distinct transformation configurations remains manageable relative to the number of snippets.

These transformations are not treated as independent experimental factors. Instead, they serve as different realizations of the treatment condition. They are included in the analysis to account for heterogeneity in how the treatment is implemented.

\subsubsection{Dataset Construction}

We restrict the study to a single programming language to control for language as a potential confounding variable (see Section~\ref{sec:confounders}). We select Java due to its widespread use in prior code comprehension studies~\cite{Wyrich:ACS23} and its continued prevalence in industry~\cite{TIOBEIndex}.

The snippets are minimal Java program that includes a targeted method for which two semantically equivalent versions (high and low verifiability) will be created, and the comprehension tasks will be designed for that particular method. 

The target method must satisfy the following constraints:
\begin{itemize}
    \item all types used in the method are from the JDK,
    \item the method is static,
    \item the method has at least one parameter and returns a value,
    \item the method contains a moderate number of lines of code to ensure a manageable task duration.
\end{itemize}

When the methods with these constraints are found, we will use slicers like Specimen~\cite{nguyen2025static} to create the snippet the includes that method. Specimen constructs a smaller program that guarantees that a typechecker will produce the same result on the sliced program when considering only the target method—that is, a type-directed slicer preserves the target program’s compile-time semantics, from the view of a specific typechecker, with respect to the target method. This is required because in order to be able to run the verification tools on it, we need to have a compilable program that includes the target method.

To choose these methods, 

\subsubsection{Participants}

Participants are undergraduate computer science students with prior experience in Java programming. Eligibility criteria include completion of relevant programming courses and familiarity with basic control flow and data structures.

Participants are recruited through standard academic channels such as course announcements and email invitations. All participants are at least 18 years old and provide informed consent prior to participation.

\subsubsection{Experimental Procedure}

The study is conducted in a single session using a web-based interface.

Each participant completes tasks on a sequence of code snippets presented one at a time. For each snippet, the participant is shown the code and asked to determine the output for a given input. The code remains visible during the task to avoid confounding memory effects.

Each participant sees only one version of each underlying snippet, either the high-verifiability or the low-verifiability version. Assignment of versions is randomized across participants so that, for each snippet, both versions are observed across the participant pool.

The order of snippets is randomized for each participant to mitigate order effects. Participants are not allowed to return to previous tasks once they proceed.

The interface records:
\begin{itemize}
    \item time spent reading the code,
    \item time spent answering the question,
    \item the final answer provided by the participant.
\end{itemize}

Participants may pause the timer when not actively working. Short breaks may be incorporated to reduce fatigue.

\subsubsection{Control of Confounding Factors}

The study design incorporates several mechanisms to control potential confounding factors.

Structural properties of the code are controlled by selecting transformations that minimally alter code length and control flow. Any residual variation in code size or complexity is measured and can be included as a covariate in the analysis.\todo{How will you measure complexity? Don't say ``cyclomatic complexity''.}

Snippet-specific difficulty is controlled through the paired design, where each underlying method appears in both treatment conditions across participants. Snippet is included as a random effect in the statistical model.

Participant-specific differences in ability and speed are addressed by including participant as a random effect. Background information may also be collected and used as additional covariates.\todo{Why ``may''? I think you should definitely do this. What's the harm?}

Order effects such as fatigue or learning are mitigated through randomization and are explicitly modeled using snippet order as a covariate.

\subsubsection{Statistical Analysis}

We analyze the data using linear mixed-effects models.\todo{Cite the specific model or models you plan to use - I think you need to convince the committee they are reasonable, so you should know exactly what you are planning here.}\todo{Justify why a linear mixed-effects model is correct in this setting.}

The primary model includes:
\begin{itemize}
    \item a fixed effect for verifiability condition,
    \item random intercepts for participants and snippets,
    \item a covariate for snippet order.
\end{itemize}

Additional fixed effects may be included to account for variation across checkers and transformations. Interaction terms can be used to explore whether the effect of verifiability differs across these dimensions.\todo{Include everything you mentioned in earlier sections here, for consistency. E.g., background effects, code size delta, ...}

The primary quantity of interest is the effect of verifiability condition on comprehension time. Statistical significance is assessed using standard inferential procedures on the model coefficients.

\subsection{Implications}
\todo{Discuss what we might conclude, given different possible observations, and discuss the implications. In particular, you want to draw out the argument here that this study is interesting and creates new knowledge no matter how it turns out: if we don't find a connection, we can argue that the FSE23 result was ephemeral; if we do, we've opened a whole new research subarea.}

\subsection{control for confounders}
\label{sec:confounder-control}
The study design incorporates several mechanisms to control for confounding factors across participant, snippet, and experimental dimensions (See Section~\ref{sec:confounders} for a full list of confounders). Participant-level confounders, such as programming experience, domain knowledge, and general ability, are partially controlled through eligibility criteria that ensure a relatively homogeneous population of undergraduate students with prior Java experience. Remaining variability across participants is accounted for statistically by including participant as a random effect, and additional background measures (e.g., self-reported experience) can be incorporated as covariates. Snippet-level confounders, including inherent difficulty, code structure, and familiarity effects, are controlled through a paired design in which each underlying method appears in both high- and low-verifiability conditions across participants, ensuring that comparisons are made between semantically equivalent versions of the same code. Transformations are carefully selected to preserve semantics while minimizing changes to code length, control flow, and layout, thereby reducing the influence of structural differences; any residual variation (e.g., size or complexity) is measured and can be included as a covariate in the analysis. Experimental confounders such as learning effects, fatigue, and ordering effects are mitigated by randomizing the order of snippet presentation for each participant and by limiting session length, while order is also explicitly modeled as a covariate. Instrumentation-related confounders are reduced by using a standardized web-based interface with consistent task presentation, timing mechanisms, and instructions for all participants. Selection bias is constrained by uniform recruitment procedures, and the between-subjects assignment of snippet versions prevents direct comparison effects and reduces hypothesis guessing. Finally, the verification context is held constant by fixing the set of tools, checkers, and configurations used to define verifiability, ensuring that observed differences in comprehension can be attributed to the manipulated verifiability condition rather than variation in the verification environment. Together, these design choices aim to isolate the effect of verifiability while minimizing the influence of alternative explanations.

\subsection{threts to validity}

\subsubsection{Summary}

The study is a controlled experiment that estimates the effect of verifiability on comprehensibility within a fixed verification ecosystem. Verifiability is manipulated through carefully designed, semantics-preserving transformations that induce false positive warnings. The design balances control and generalizability by fixing key aspects of the verification setting while sampling multiple transformations. Mixed-effects analysis is used to account for variability across participants and snippets and to isolate the effect of the treatment.

